# 三端兼容与测试矩阵

更新时间：2026-08-20

## 2026-08-20 本轮平台按钮即时状态

- Android 0.6.26/code 64 与 iPhone 0.6.13/build 32：点击“发抖音”或“发小红书”后对应按钮立即变灰但仍可点击；两个平台的灰色状态和点击次数互不影响，返回作品页会按已保存状态恢复。
- Android/iOS 的云端构建、公开 Release、`latest.json`、桌面安装包和真实手机安装/更新必须分别核对；本地 Windows 不宣称 iOS 真机构建或真机通过。

## 2026-08-19 本轮按钮层级优化候选

- Android 0.6.25/code 63 与 iPhone 0.6.12/build 31：作品卡片的“预览 / 发抖音 / 发小红书”改为左对齐纵向紧凑按钮，按钮按文案自适应宽度；Android 继续支持上一张/下一张，iPhone 继续支持左右滑动多图预览。
- Android/iOS 的云端构建、公开 Release、`latest.json`、桌面安装包和真实手机安装/更新必须分别核对；提交前不把候选写成真机通过。

## 2026-08-18 本轮云端交付候选

- Android 0.6.24/code 62 与 iPhone 0.6.11/build 30：移除自动截图和自动剪切板同步；作品卡片统一为“预览 / 发抖音 / 发小红书”，Android 支持上一张/下一张，iPhone 支持左右滑动多图预览。
- Android/iOS 的云端构建、公开 Release、`latest.json`、桌面安装包和真实手机安装/更新必须分别核对；当前代码提交前不把候选写成真机通过。

## 不拆包原则

- Android 始终使用 `com.zwm.gallery`、同一签名和同一升级链；不制作红米版、华为版。
- iPhone 始终使用 `com.zwm.album`；同一 IPA 根据系统能力切换目录方式。
- Windows 只有一套原生面板；作品识别不是传输前置条件，任意文件都能传。
- 修复旧设备时必须同时保留新设备路径，自动化用例至少覆盖公共逻辑与系统分支。

## 当前能力矩阵

| 平台/设备 | 系统路径 | 当前结果 |
|---|---|---|
| Android 通用包 0.6.14 | Android 8～16 / SAF + MediaStore + 系统分享 | 文件夹入口、分类滑块、可见下拉刷新、胶囊开关和截图主设备选择保持；更新采用“自动检查、确认后系统下载、点系统通知安装”，不申请安装来源权限。手机发送可选择自动/USB/Wi‑Fi；USB 指系统 USB 网络共享，普通 MTP 仍由电脑主动写手机。系统下载与 USB 双向均需实体逐机复核。 |
| Redmi 9A | Android 11 / MIUI 12.5 / SAF | 同一签名覆盖到 0.5.0，原目录授权、作品和接收设置保留；Wi-Fi 断开/恢复后自动重建发现。作品详情、3:4 预览、长按多选和三图标操作完成实体操作。 |
| Redmi Note 8 | Android 10 / SAF + 隐藏目录兼容通道 | 2026-08-01 同签名覆盖到 0.6.8/code 46；首页左侧文件夹入口、四分类滑块文字、分类切换、下拉四段状态与旋转进度、完整胶囊开关、主设备选择弹窗完成实体操作。原设置和目录覆盖后保留。 |
| Huawei Nova / P30 / HarmonyOS、EMUI | SAF + Android 10 文件兼容通道 | 0.3.9 曾完成隐藏作品 3+4=7 的实体目录检查；P30 当前 0.5.2 在局域网上报 27 个作品。0.5.4 增加选择器结果先到、目标回调后到的时序兼容与自动回归；该机覆盖安装和真实分享计数需 USB 重新枚举后操作复核。 |
| 较新 Redmi | 标准 SAF | Android 0.4.1 已覆盖安装；两列卡片、分享次数、回收站实体移动/恢复/清空和三端发现均完成实机操作 |
| Redmi K60 / VIVO 应用分身 | Android 10+ MediaStore 分享桥 | 红米 K60 分身实际进入 10 图编辑页；VIVO 0.5.0 实测分身冷启动会先返回，再由目标应用延迟打开，新等待逻辑识别真正打开后才计数。两者共用同一兼容包。 |
| iPhone 6 | iOS 12 App 内接收目录与子目录选择 | 0.4.5 build 16 已在 iOS 12.5.8 覆盖安装。实体复现并修复数字重名回收站导致的重复作品；临时放入 1/2 个“图片 + TXT”目录时首页数字对应为 1/2，删除测试目录后恢复 0。设置页选择器的目录枚举与保存逻辑由自动测试覆盖。 |
| 较新 iPhone | iOS 13+ 外部文件夹授权或固定目录 | iPhone 12 / iOS 26.5 已沿原 bundle id 覆盖到 0.4.7 build 18；启动后作品数为 23，电脑与共享技能均显示“苹果12（作品数 23）”。旧版传输、接收与目录授权数据继续可读。 |
| Windows 10/11 | Win32 + WinHTTP | V3.7 统一手机端图标、平台标识、圆角卡片与操作按钮；备用构建 run `29968611397` 正式编译成功。旧版手机仍可发现和传送，不显示虚构数字。 |
| iPhone 0.6.7 / Windows V4.1.2 | iOS 前台 + Win32 局域网 | iPhone 去掉重复总量、新增文件浏览、重排剪切板/截图设置，并删除重复顶部刷新按钮；macOS CI 真机 SDK 构建已通过并已发布 IPA。iPhone 12 / iOS 26.5.2 已完成同标识覆盖安装，设备清单确认 0.6.7/build 26，直接启动、设置页和首页截图通过；首页确认无刷新按钮、无重复总量，四分类数量及文件夹入口正确。分类动画与下拉刷新手感仍需触屏验收。Windows 本轮无代码变化。 |
| iPhone 0.6.8 / Windows V4.3.4 | iOS 前台 + Win32 局域网 | run `31235217862` 四项任务全绿；iOS IPA 包内确认 0.6.8/build 27、最低 iOS 12，实体仍待用户签名覆盖安装。Windows V4.3.4 已替换桌面旧版并真实打开独立设置窗口；GitHub API 限流状态下 Release 跳转兜底实测成功。 |

## iOS 系统能力边界

- iOS 12 没有“给第三方 App 持久授权任意外部文件夹”的系统能力，因此 iPhone 6 使用应用 Documents 目录；0.4.4 可在 App 内选择 Documents 本身或任意已接收子目录作为扫描根目录。系统“文件”App 不是接收和扫描的前置条件。
- iOS 13 及以上可选择外部总文件夹，也可在设置中切回应用 Documents 目录。
- 两条路径共用 `WorkScanner`：最多递归 8 层、1000 个文件夹，同时含图片与 TXT 才是作品。

## 通用传输与作品识别的边界

1. Windows 负责把普通文件直接发送，把电脑文件夹临时整理为保留相对路径的 ZIP。
2. Android 接收层把所有内容写入用户设置的接收目录；iPhone 仅展开 Windows 为文件夹传输生成的存储式 ZIP。两端都拒绝绝对路径和 `..` 越界。
3. 作品扫描层只把“同一目录中至少一张图片 + 一个 TXT”加入作品列表。
4. PDF、视频、表格、文档、源文件等不会被误判成作品，但仍会完整保留。

## 已踩坑与回归要求

- SAF 与 Android 10 隐藏目录通道可能看到同一个作品：必须按真实来源路径去重，不能只比较内部编号。
- 多个 `WorkLibrary` 同时启动会竞争历史迁移：迁移必须进程内串行化，并保留并发回归测试。
- Windows ZIP 可能用反斜杠表示目录项：目录判断同时接受 `/` 与 `\`，否则会生成同名空文件和“(1)”目录。
- macOS CI runner 不一定预装可启动的 iPhone 模拟器：有运行时就执行测试；没有时至少编译 App 与测试包，再执行真机 SDK 构建。
- iPhone 6 不能依赖 SwiftUI 生命周期、NavigationStack、SF Symbols 或 iOS 13 动态系统色；兼容 UI 使用 UIKit 和版本保护。
- 实机测试数据完成后必须清理，并再次核对用户原作品数量，避免测试素材污染作品库。
