name: Album
packages:
  WebRTC:
    url: https://github.com/stasel/WebRTC.git
    exactVersion: 151.0.0
options:
  bundleIdPrefix: com.zwm
  deploymentTarget:
    iOS: "12.0"
settings:
  base:
    SWIFT_VERSION: "5.0"
    TARGETED_DEVICE_FAMILY: "1"
    CODE_SIGN_STYLE: Automatic
targets:
  Album:
    type: application
    platform: iOS
    sources:
      - path: Album
    dependencies:
      - package: WebRTC
        product: WebRTC
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zwm.album
        # Keep bundle/executable paths ASCII-only. The user-facing name stays Chinese
        # via CFBundleDisplayName, avoiding IPA ZIP filename encoding failures.
        PRODUCT_NAME: Album
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CURRENT_PROJECT_VERSION: 64
        MARKETING_VERSION: 0.6.45
        SWIFT_OBJC_BRIDGING_HEADER: Album/Album-Bridging-Header.h
        OTHER_LDFLAGS: "$(inherited) -lz"
    info:
      path: Album/Info.plist
      properties:
        CFBundleDisplayName: 相册
        CFBundleName: Album
        CFBundleShortVersionString: "0.6.45"
        CFBundleVersion: "64"
        NSLocalNetworkUsageDescription: 用于让同一 Wi-Fi 的素材投送中控自动发现这台 iPhone，并把文件直接传入所选作品文件夹。
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        LSApplicationCategoryType: public.app-category.productivity
        LSSupportsOpeningDocumentsInPlace: true
        UIFileSharingEnabled: true
        UIApplicationSupportsIndirectInputEvents: true
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
  AlbumTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: AlbumTests
    dependencies:
      - target: Album
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
