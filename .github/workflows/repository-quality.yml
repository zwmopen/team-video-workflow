name: Repository quality

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check Python syntax
        run: python -m compileall -q tools skill tests

      - name: Run filename, traceability, filesystem, transaction, and browser security contracts
        run: python -m unittest discover -s tests -v

      - name: Verify source collection safety markers
        run: |
          python - <<'PY'
          from pathlib import Path

          collector = Path('tools/teambuilding-video-scene-library/src/tb_scene/source_collector.py').read_text(encoding='utf-8-sig')
          cli = Path('tools/teambuilding-video-scene-library/src/tb_scene/cli.py').read_text(encoding='utf-8-sig')

          required = [
              'move_files: bool = False',
              'move_bundle_transactionally',
              'would_quarantine_source_duplicate',
              'Source and output roots must not be nested',
          ]
          for marker in required:
              if marker not in collector:
                  raise SystemExit(f'Missing source collector safety marker: {marker}')

          for marker in ['delete_source_duplicate', '.unlink(']:
              if marker in collector:
                  raise SystemExit(f'Forbidden destructive source collector pattern remains: {marker}')

          for command in ['refine', 'collect', 'clean', 'rename']:
              marker = f'{command}.add_argument("--move", type=parse_bool, default=False'
              if marker not in cli:
                  raise SystemExit(f'{command} command is not Preview-first')
          PY

      - name: Verify scene filesystem and SQLite transaction contracts
        run: |
          python - <<'PY'
          from pathlib import Path

          clip = Path('tools/teambuilding-video-scene-library/src/tb_scene/clip_renamer.py').read_text(encoding='utf-8-sig')
          visual = Path('tools/teambuilding-video-scene-library/src/tb_scene/visual_corrector.py').read_text(encoding='utf-8-sig')
          cli = Path('tools/teambuilding-video-scene-library/src/tb_scene/cli.py').read_text(encoding='utf-8-sig')

          clip_required = [
              'move_files: bool = False',
              "confirm_token != \"RENAME\"",
              'BEGIN IMMEDIATE',
              'con.rollback()',
              'filesystem rollback was incomplete',
          ]
          visual_required = [
              'apply: bool = False',
              "confirm_token != \"APPLY\"",
              'BEGIN IMMEDIATE',
              'cleanup_created_directories',
              'con.rollback()',
              'filesystem rollback was incomplete',
          ]
          cli_required = [
              'rename.add_argument("--confirm-token"',
              'visual_fix.add_argument("--apply", type=parse_bool, default=False',
              'visual_fix.add_argument("--confirm-token"',
              'visual_fix.add_argument("--rename-after"',
          ]

          for marker in clip_required:
              if marker not in clip:
                  raise SystemExit(f'Missing clip transaction marker: {marker}')
          for marker in visual_required:
              if marker not in visual:
                  raise SystemExit(f'Missing visual transaction marker: {marker}')
          for marker in cli_required:
              if marker not in cli:
                  raise SystemExit(f'Missing CLI transaction marker: {marker}')
          PY

      - name: Verify local browser API security contracts
        run: |
          python - <<'PY'
          from pathlib import Path

          source = Path('tools/team-video-library-browser/server.py').read_text(encoding='utf-8-sig')
          required = [
              'ThreadingHTTPServer(("127.0.0.1", port)',
              'is_trusted_local_request',
              'MAX_JSON_BODY_BYTES = 1024 * 1024',
              'Cross-Origin-Resource-Policy',
              'resolve_allowed_file',
              'confirm:"RENAME"',
              'confirm:"RECYCLE"',
              'write_json_atomically',
          ]
          for marker in required:
              if marker not in source:
                  raise SystemExit(f'Missing browser security marker: {marker}')

          forbidden = [
              'errors="ignore") if length else "{}"',
              'postJson("/api/delete", {id:item.id})',
              'postJson("/api/rename", {id:item.id, name: requested})',
          ]
          for marker in forbidden:
              if marker in source:
                  raise SystemExit(f'Forbidden browser API pattern remains: {marker}')
          PY

      - name: Check whitespace errors
        run: git diff --check

      - name: Reject tracked files larger than 50 MB
        run: |
          python - <<'PY'
          import subprocess
          from pathlib import Path

          limit = 50 * 1024 * 1024
          files = subprocess.check_output(['git', 'ls-files', '-z']).decode().split('\0')
          large = []
          for item in filter(None, files):
              path = Path(item)
              if path.is_file() and path.stat().st_size > limit:
                  large.append(f'{item} ({path.stat().st_size} bytes)')
          if large:
              raise SystemExit('Files larger than 50 MB are not allowed:\n' + '\n'.join(large))
          PY
