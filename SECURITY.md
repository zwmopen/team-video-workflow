# Security Policy

## 数据与文件安全

- 不提交视频、音频、缩略图、转录缓存、模型、客户资料或账号凭据。
- 原片不得被覆盖或删除；处理结果必须写入新路径或可回滚暂存区。
- 批量移动、重命名和删除必须生成报告，并保留来源视频 ID 与分镜 ID。
- 所有具有破坏性的命令默认必须是 Preview；只有显式 Apply/Move 参数才能修改文件。
- 原视频及其同名字幕、文案、图片等 sidecar 必须作为事务包移动，失败时按反向顺序恢复。
- 精确重复素材必须进入隔离目录，禁止通过 `unlink` 或永久删除实现去重。
- 输入根目录与输出根目录不得相同或相互嵌套。
- 所有用户提供路径都必须限制在允许的素材根目录内。
- 示例路径不得包含真实客户信息或个人凭据。

## 分镜文件与数据库一致性

- 视觉纠错和分镜批量改名默认只生成计划，不得隐式修改文件或 SQLite。
- 正式视觉纠错必须提供确认令牌 `APPLY`；正式分镜改名必须提供确认令牌 `RENAME`。
- 文件移动/改名与 SQLite 更新必须在同一逻辑事务内执行：SQLite 使用显式事务，文件操作记录已完成步骤。
- 任一数据库更新、文件移动或完整性检查失败时，必须回滚 SQLite，并按反向顺序恢复已移动文件。
- 事务执行前必须预检源文件、目标冲突、路径越界和数据库目标行；数据库更新必须确认恰好影响一条记录。
- 事务中新建的目录在失败后仅可在为空时删除，禁止误删已有目录内容。
- 报告、CSV 和导出文件属于核心事务后的派生输出；派生失败应返回警告，不得回滚已经成功提交的核心事务，也不得伪报事务成功前的状态。
- 故障注入测试必须覆盖：移动后失败、数据库回滚、文件反向恢复、成功提交和 Preview 不落盘。

## 本地素材浏览器

- HTTP 服务固定监听 `127.0.0.1`，不得暴露到局域网或公网。
- 每个请求必须校验本机 Host；带有 `Origin` 或 `Referer` 时必须来自本机同端口页面；`Sec-Fetch-Site: cross-site` 必须拒绝。
- 所有 POST 必须使用 `application/json`，请求体不得超过 1 MB。
- 改名和移到回收站不仅依赖前端弹窗，还必须在请求体中携带服务端确认令牌。
- 浏览器只能发送素材库或浏览器缓存允许根目录内的真实文件，禁止通过路径穿越或符号链接读取目录外文件。
- 符号链接素材不得执行改名或删除，避免操作链接指向的意外目标。
- 标签、裁剪布局和其他浏览器状态必须使用临时文件加原子替换写入。
- HTTP 响应必须设置 `nosniff`、禁止 iframe、禁止 Referrer 和同源资源策略。
- 缓存清理必须跳过符号链接，并且只能删除已知缓存根目录中的生成物。
- 自动测试必须覆盖 Host 欺骗、跨站请求、非 JSON、超大请求、媒体真实路径边界、原子 JSON 写入和越界文件改名拒绝。

## 报告问题

不要在 Issue 或 PR 中上传敏感素材。请通过仓库 Security 页面或 GitHub 私下联系维护者，说明受影响工具、操作路径和潜在数据损失范围。

发现凭据泄漏时，先轮换凭据；发现文件操作风险时，立即停止批处理并保留日志和现场目录。
